function not:
read Arg1
%
    if Arg1 then
        Result := nil
    else
        Result := cons( nil nil )
    fi
%
write Result

function stdlibtestADD:
read Arg1,Arg2
%
    if Arg1 then
        Result := nil
    else
        Result := nil
    fi
%
write Result